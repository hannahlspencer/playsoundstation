package Function;

public class FunctionUtils {


    /**
     * compares the key of the existing string (based off mood) and the key recommended for the function
     * selected, and ensures they're compatible
     * @return
     */
    private String compareKeys() {
        return "x";
    }
    /**
     * Ensures that for the success and fail functions the tail end of the score goes up or down in pitch respectively
     * @param score
     * @return checked score
     */
    private String checkNoteTail(String score) {
        return score;
    }
}
